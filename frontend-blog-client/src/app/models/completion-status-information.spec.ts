import { CompletionStatusInformation } from './completion-status-information';

describe('RegistrationResult', () => {
  it('should create an instance', () => {
    expect(new CompletionStatusInformation()).toBeTruthy();
  });
});
